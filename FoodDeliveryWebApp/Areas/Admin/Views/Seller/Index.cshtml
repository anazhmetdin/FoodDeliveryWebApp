@model IEnumerable<Seller>
@{
    ViewData["Title"] = "Index";
}


<div class="container p-3">
    @*<div class="row pt-4">
    <div class="col-6">
    <h2 class="text-primary">Sellers List</h2>
    </div>
    </div>
    <div class="col-6 text-end">
    <a asp-controller="Category" asp-action="Create" class="btn btn-primary">
    <i class="bi bi-plus-circle"></i> &nbsp; Create New Category
    </a>
    </div>
    </div>
    <br /><br />*@

    <table class="table table-bordered table-striped" style="wIdth:100%">
        <thead>
            <tr>
                <th>
                    Store Name
                </th>
                <th>
                    Seller Status
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var seller in Model)
            {
                <tr>
                    <td width="50%">
                        @seller.StoreName
                    </td>
                    <td width="30%">
                        @seller.Status
                    </td>
                    <td>
                        <div class="w-75 btn-group" role="group">
                            <a asp-area="Admin" asp-controller="Seller" asp-action="Edit" asp-route-id="@seller.Id"
                               class="btn btn-primary mx-2"> <i class="bi bi-pencil-square"></i> Edit</a>
                            <a asp-area="Admin" asp-controller="Seller" asp-action="Delete" asp-route-id="@seller.Id"
                               class="btn btn-danger mx-2"> <i class="bi bi-trash-fill"></i> Delete</a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>

    </table>
</div>
